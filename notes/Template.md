Motivation for templates: write more **generic** codes
Define a function that allows different data types of input so that there is no need to write multiple implements for a function.
**Template is a "special class" where the data type is a parameter**

## Define a template
```cpp=
template <typename T>
```

OR, equivalently, 
```cpp=
template <class T>
```
